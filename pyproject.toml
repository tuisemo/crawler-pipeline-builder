[project]
name = "sea-data-workspace"
version = "0.1.0"
description = "DSL-based crawler orchestration workbench with workflow APIs and React frontend"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "packages/page-extractor",
    "backend",
]
